<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="com.test.videosstories.presentation.home.HomeViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <Button
            android:id="@+id/btn_videos"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/dp_16"
            android:onClick="@{() -> viewModel.onBtnAllVideosClicked()}"
            android:text="Videos"
            app:layout_constraintTop_toTopOf="parent" />

        <Button
            android:id="@+id/btn_stories"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/dp_16"
            android:text="Stories"
            app:layout_constraintTop_toBottomOf="@id/btn_videos" />

        <Button
            android:id="@+id/btn_all"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/dp_16"
            android:text="All videos and stories"
            app:layout_constraintTop_toBottomOf="@id/btn_stories" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>